apiVersion: v1
kind: Service
metadata:
  name: {{ include "wanderlust.fullname" . }}
  labels:
    {{- include "wanderlust.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wanderlust.selectorLabels" . | nindent 4 }}
