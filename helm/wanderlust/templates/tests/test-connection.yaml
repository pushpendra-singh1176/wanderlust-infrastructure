apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wanderlust.fullname" . }}-test-connection"
  labels:
    {{- include "wanderlust.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wanderlust.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
