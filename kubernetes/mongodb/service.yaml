apiVersion: v1
kind: Service

metadata:
  name: mongodb-service
  namespace: wanderlust

  labels:
    app: mongodb
    tier: database

spec:
  clusterIP: None

  selector:
    app: mongodb

  ports:
    - name: mongodb
      port: 27017
      targetPort: 27017

      